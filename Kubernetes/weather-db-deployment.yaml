---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: weather-db-part0
  name: weather-db-part0
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: weather-db-part0
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.network/microservices-network: "true"
        io.kompose.service: weather-db-part0
    spec:
      containers:
        - env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
          image: mircoantona/weathereventnotifier:weather-db-part0
          name: weather-db-part0
          ports:
            - containerPort: 3306
              hostPort: 3312
              protocol: TCP
          resources: {}
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: weather-database-part0
      restartPolicy: Always
      volumes:
        - name: weather-database-part0
          persistentVolumeClaim:
            claimName: weather-database-part0
status: {}

---

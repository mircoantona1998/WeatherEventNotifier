---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: telegram-db
  name: telegram-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: telegram-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.network/microservices-network: "true"
        io.kompose.service: telegram-db
    spec:
      containers:
        - env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
          image: mircoantona/weathereventnotifier:telegram-db
          name: telegram-db
          ports:
            - containerPort: 3306
              hostPort: 3311
              protocol: TCP
          resources: {}
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: telegram-database
      restartPolicy: Always
      volumes:
        - name: telegram-database
          persistentVolumeClaim:
            claimName: telegram-database
status: {}

---
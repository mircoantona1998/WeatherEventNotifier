---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: configurator-db
  name: configurator-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: configurator-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.network/microservices-network: "true"
        io.kompose.service: configurator-db
    spec:
      containers:
        - env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
          image: mircoantona/weathereventnotifier:configurator-db
          name: configurator-db
          ports:
            - containerPort: 3306
              hostPort: 3307
              protocol: TCP
          resources: {}
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: configurator-database
      restartPolicy: Always
      volumes:
        - name: configurator-database
          persistentVolumeClaim:
            claimName: configurator-database
status: {}

---

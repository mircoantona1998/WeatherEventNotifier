apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app.service: sla-manager-db
  name: sla-manager-db
spec:
  serviceName: "sla-manager-db"
  replicas: 1
  selector:
    matchLabels:
      app.service: sla-manager-db
  template:
    metadata:
      labels:
        app.network/microservices-network: "true"
        app.service: sla-manager-db
    spec:
      containers:
        - name: sla-manager-db
          image: mcr.microsoft.com/mssql/server
          env:
            - name: ACCEPT_EULA
              value: "Y"
            - name: MSSQL_PID
              value: Express
            - name: SA_PASSWORD
              value: RootRoot.1
          ports:
            - containerPort: 1433
              hostPort: 1434
          volumeMounts:
            - mountPath: /var/opt/mssql
              name: sla-manager-database
  volumeClaimTemplates:
    - metadata:
        name: sla-manager-database
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi 

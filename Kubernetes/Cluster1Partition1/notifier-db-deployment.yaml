---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: notifier-db
  name: notifier-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: notifier-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.network/microservices-network: "true"
        io.kompose.service: notifier-db
    spec:
      containers:
        - env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
          image: mircoantona/weathereventnotifier:notifier-db
          name: notifier-db
          ports:
            - containerPort: 3306
              hostPort: 3309
              protocol: TCP
          resources: {}
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: notifier-database
      restartPolicy: Always
      volumes:
        - name: notifier-database
          persistentVolumeClaim:
            claimName: notifier-database
status: {}

---
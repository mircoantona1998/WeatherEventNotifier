apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app.service: notifier-db
  name: notifier-db
spec:
  serviceName: "notifier-db"
  replicas: 1
  selector:
    matchLabels:
      app.service: notifier-db
  template:
    metadata:
      labels:
        app.network/microservices-network: "true"
        app.service: notifier-db
    spec:
      containers:
        - name: notifier-db
          image: mircoantona/weathereventnotifier:notifier-db
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
          ports:
            - containerPort: 3306
              hostPort: 3309
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: notifier-database
  volumeClaimTemplates:
    - metadata:
        name: notifier-database
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi  
